Verificar possíveis erros nos códigos abaixo: 

package org.example;

public abstract class Personagem {

    private String nome;
    private int id;

    public Personagem(String nome, int id) {
        this.nome = nome;
        this.id = id;
    }

    public String getNome() {
        return nome;
    }

    public int getId() {
        return id;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public abstract void apresentar();
}

package org.example;

public class Mago extends Personagem {

    public Mago(String nome, int id) {
        super(nome, id);
    }

    @Override
    public void apresentar() {

        System.out.println(
                "Eu sou o mago de fogo " +
                getNome() +
                " tenho " +
                getId() +
                " anos."
        );

        System.out.println(
                "Minha habilidade específica é explosion."
        );
    }
}
package org.example;

public class Divisao {

    String nome;
    String funcao;

    public Divisao(String nome, String funcao) {
        this.nome = nome;
        this.funcao = funcao;
    }
}

package org.example;

import java.util.ArrayList;

public class Castelo {

    ArrayList<Divisao> divisoes;

    public Castelo(ArrayList<Divisao> divisoes) {
        this.divisoes = divisoes;
    }

    public void adicionarDivisao(Divisao divisao) {
        divisoes.add(divisao);
    }

    public void listarDivisoes() {

        System.out.println("\nDivisões do Castelo:");

        for (Divisao comodo : divisoes) {
            System.out.println(
                    "- " +
                    comodo.nome +
                    " : " +
                    comodo.funcao
            );
        }
    }
}

package org.example;

import java.util.ArrayList;

public class Main {


        Mago howl = new Mago("Howl", 18);
        howl.apresentar();

        Divisao quarto =
                new Divisao("Quarto", "Dormir");

        Divisao laboratorio =
                new Divisao("Laboratorio", "Pesquisa");

        Divisao quadra =
                new Divisao("Arena", "Treinamento");

        ArrayList<Divisao> lista =
                new ArrayList<>();

        lista.add(quarto);
        lista.add(laboratorio);

        Castelo castelo = new Castelo(lista);

        castelo.adicionarDivisao(quadra);

        castelo.listarDivisoes();
    }
}


Main.java

Aqui estava o problema.

Tudo precisa ficar dentro do:

public static void main(String[] args)

------------------------------------------------------------------------------------

cafe package org.example; public class Cafe { private String nome; private double preco; public Cafe(String nome, double preco) { this.nome = nome; this.preco = preco; } public String getNome() { return nome; } public double getPreco() { return preco; } public double calcularPrecoFinal() { return preco; } } Cafe gorumet package org.example; public class CafeGourmet extends Cafe { private double bonusAroma; public CafeGourmet(String nome, double preco, double bonusAroma) { super(nome, preco); this.bonusAroma = bonusAroma; } @Override public double calcularPrecoFinal() { return getPreco() + bonusAroma; } } Cafeteria package org.example; public class CafeteriaLeblanc { private Menu menu; public CafeteriaLeblanc() { menu = new Menu(); } public void adicionarCafe(Cafe cafe) { menu.adicionarCafe(cafe); } public void abrirCafeteria() { System.out.println("Bem vindo a cafeteria Leblanc!"); menu.exibirMenu(); } } } Main package org.example; //TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or // click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter. public class Main { public static void main(String[] args) { Cafe opcao1 = new Cafe("Cafe curto", 4.0); Cafe opcao2 = new Cafe("Cafe Longo", 8.0); CafeGourmet opcao3 = new CafeGourmet( "Capuccino com canela", 10.0, 2.0 ); CafeteriaLeblanc cafeteria = new CafeteriaLeblanc(); CafeteriaLeblanc.adicionarCafe(opcao1); CafeteriaLeblanc.adicionarCafe(opcao2); CafeteriaLeblanc.adicionarCafe(opcao3); leblanc.abrirCafeteria(); } } Menu package org.example; public class Menu { private ArrayList<Cafe> cafes; public Menu() { cafes = new ArrayList<>(); } public void adicionarCafe(Cafe cafe) { cafes.add(cafe); } public void exibirMenu() { System.out.println("MENU"); for (Cafe cafe : cafes) { System.out.println(cafe.getNome() + " - R$ " + cafe.calcularPrecoFinal() ); } } }

Seu código está quase certo, mas tem vários erros pequenos de Java que impedem a compilação no IntelliJ. Vou te mostrar exatamente o que está errado e já te entregar a versão corrigida.

❌ ERROS PRINCIPAIS
1. Falta import no Menu

Você usa ArrayList, mas não importou:

import java.util.ArrayList;
2. Erro no Main (MAIS IMPORTANTE)

Você escreveu:

CafeteriaLeblanc.adicionarCafe(opcao1);

❌ Isso está errado porque adicionarCafe não é static.

✔ Correto é usar o objeto:

cafeteria.adicionarCafe(opcao1);
3. Variável inexistente

Você fez:

leblanc.abrirCafeteria();

❌ Mas não existe leblanc.

✔ O nome correto é:

cafeteria.abrirCafeteria();
4. Menu sem import

Também faltou:

import java.util.ArrayList;

------------------------------------------------------------------------------------------------------------------
package org.example; public class Main { public static void main(String[] args) { // Criando aliens Alien alien1 = new Alien( "Frieza", "Rajada de Energia", 100 ); Alien alien2 = new Alien( "Boros", "Explosão Etelar", 95 ); // Criando youkais Youkai youkai1 = new Youkai( "Kurama", "Chicote de Rosa", 85 ); Youkai youkai2 = new Youkai( "Hiei", "Chama Negra do Dragao", 95 ); RegistroOculto registro = new RegistroOculto(); registro.registrarEntidade(alien1); registro.registrarEntidade(alien2); registro.registrarEntidade(youkai1); registro.registrarEntidade(youkai2); registro.registrarEntidade(alien1); registro.listarEntidades(); } } Alien package org.example; public class Alien extends Entidade implements Rastravel{ private Poder poder; public Alien(String nome, String nomePoder, int intensidade) { super(nome); // composição poder = new Poder(nomePoder, intensidade); } @Override public String obterCoordenadas() { return "Area51 X:3724804 Y:155800"; } } Entidade package org.example; public class Entidade { private String nome; public Entidade(String nome) { this.nome = nome; } public String getNome() { return nome; } } } Poder package org.example; public class Poder { String nome; int intensidade; public Poder(String nome, int intensidade) { this.nome = nome; this.intensidade = intensidade; } } Rastravel package org.example; public interface Rastravel { String obterCoordenadas(); } Registro oculto package org.example; public class RegistroOculto { private HashMap<String, Entidade> entidades; public RegistroOculto() { entidades = new HashMap<>(); } public void registrarEntidade(Entidade entidade) { if (entidades.containsKey(entidade.getNome())) { System.out.println("A entidade " + entidade.getNome() + " já está no mapa!"); } else { entidades.put(entidade.getNome(), entidade); System.out.println(entidade.getNome() + "Adicionada!" ); } } public void listarEntidades() { System.out.println("\nENTIDADES REGISTRADAS"); for (Map.Entry<String, Entidade> item : entidades.entrySet()) { Entidade entidade = item.getValue(); Rastreavel rastreavel = (Rastreavel) entidade; System.out.println("Nome: " + entidade.getNome()); System.out.println("Coordenadas: " + rastreavel.obterCoordenadas()); System.out.println(); } } } Youkai package org.example; public class Youkai extends Entidade implements Rastravel{ private Poder poder; public Youkai(String nome, String nomePoder, int intensidade) { super(nome); poder = new Poder(nomePoder, intensidade); } @Override public String obterCoordenadas() { return "Aokigahara X:35 Y:138"; } }


Seu código está muito próximo de funcionar, mas encontrei 5 erros de compilação.

1. Erro na classe Entidade

Você tem uma chave } sobrando no final:

public class Entidade {

    private String nome;

    public Entidade(String nome) {
        this.nome = nome;
    }

    public String getNome() {
        return nome;
    }
}
}

O correto é:

package org.example;

public class Entidade {

    private String nome;

    public Entidade(String nome) {
        this.nome = nome;
    }

    public String getNome() {
        return nome;
    }
}
2. Falta importar HashMap e Map

Na classe RegistroOculto você usa:

HashMap<String, Entidade>
Map.Entry<String, Entidade>

Mas não importou as bibliotecas.

Adicione no início:

import java.util.HashMap;
import java.util.Map;
3. Nome da interface inconsistente

Você criou:

public interface Rastravel

e nas classes:

implements Rastravel

Mas em RegistroOculto escreveu:

Rastreavel rastreavel = (Rastreavel) entidade;

❌ Rastreavel não existe.

Você precisa usar exatamente o mesmo nome.

Por exemplo, mantenha tudo como:

Rastravel

e troque para:

Rastravel rastreavel = (Rastravel) entidade;
4. Espaço faltando na mensagem

Está assim:

System.out.println(entidade.getNome() + "Adicionada!");

Saída:

FriezaAdicionada!

Melhor:

System.out.println(entidade.getNome() + " adicionada!");
5. Melhorar a interface

Seu professor pediu:

Rastreavel

Você escreveu:

Rastravel

O Java aceita, mas para ficar exatamente igual ao enunciado seria melhor renomear:

public interface Rastreavel

e depois:

implements Rastreavel

em Alien e Youkai.

------------------------------------------------------------------------------------------------------------
Cogumelo package org.example; public class Cogumelo extends Mosntro { public Cogumelo(String nome) { super(nome); } @Override public void virarPrato() { System.out.println( "Senshi prepara o cogumelo assado na fogueira com sal." ); } } Lagosta package org.example; public class Lagosta extends Monstro { public Lagosta(String nome) { super(nome); } @Override public void virarPrato() { System.out.println("Senshi cozinha a lagosta com ervas."); } } Main package org.example; import java.util.ArrayList; //TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or // click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter. public class Main { public static void main(String[] args) { Lagosta lagosta = new Lagosta("Lagosta"); Cogumelo cogumelo = new Cogumelo("Cogumelo"); Prato prato1 = new Prato( "Lagosta cozida com ervas", lagosta ); Prato prato2 = new Prato( "Cogumelo assado com sal", cogumelo ); prato1.servir(); prato2.servir(); ArrayList<Prato> cardapio = new ArrayList<>(); cardapio.add(prato1); cardapio.add(prato2); System.out.println("\nCARDÁPIO DA EXPEDIÇÃO"); for (Prato prato : cardapio) { prato.servir(); } } } Monstro package org.example; public class Monstro { private String nome; public Monstro(String nome) { this.nome = nome; } public String getNome() { return nome; } public abstract void virarPrato(); } Prato package org.example; public class Prato { private String nome; private Monstro monstro; private Tempero tempero; public Prato(String nome, Monstro monstro) { this.nome = nome; this.monstro = monstro; tempero = new Tempero("Sal e Ervas", "2 colheres"); } public void servir() { System.out.println("\nPrato: " + nome); monstro.virarPrato(); System.out.println ("Tempero usado: " + tempero.nome + " - " + tempero.quantidade); } } Tempero package org.example; public class Tempero { String nome; String quantidade; public Tempero(String nome, String quantidade) { this.nome = nome; this.quantidade = quantidade; } }


Encontrei dois erros de compilação e um detalhe de estilo.

1. Erro na classe Monstro

Você declarou um método abstrato:

public abstract void virarPrato();

Mas a classe não foi declarada como abstrata.

Seu código:

public class Monstro {

Deve ser:

public abstract class Monstro {

Classe corrigida:

package org.example;

public abstract class Monstro {

    private String nome;

    public Monstro(String nome) {
        this.nome = nome;
    }

    public String getNome() {
        return nome;
    }

    public abstract void virarPrato();
}
2. Erro na classe Cogumelo

Você escreveu:

public class Cogumelo extends Mosntro {

Observe que está:

Mosntro

Mas a classe chama:

Monstro

Faltou trocar as letras de posição.

Corrija para:

public class Cogumelo extends Monstro {
3. Melhorar o método servir

Está funcionando, mas esta linha ficou estranha:

System.out.println("\nPrato: " + nome); monstro.virarPrato();

Melhor separar:

System.out.println("\nPrato: " + nome);

monstro.virarPrato();

------------------------------------------------------------------------------------------
Obs: Os apontados informados pela IA foram diretos aos pontos que estavam errados no código, ajudando a identificar e finalizar o código corretamente. Alguns apontamentos foram simples, era só ter prestado mais atenção no desenvolvimento e não trocar as informações colocadas em uma pasta para outra. Em resumo, a ajuda foi satisfatória. 